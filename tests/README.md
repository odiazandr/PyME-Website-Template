# Tests

The implemented suites cover memory contracts, data schemas, source routing, structured-data serialization, hosting security, generated artifacts, production-validator acceptance, and production-validator rejection. They run through `npm test` or the full memory-health command. Browser smoke and accessibility suites remain planned for their later implementation phase.
