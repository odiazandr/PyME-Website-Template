---
owner: docs/runbooks/active-working-set.md
authority: canonical
status: active
answers: ["How is a Working Set created and used?"]
---
# Use an Active Working Set

Name the task, add no more than the configured pointer limit, and give each path a short relevance reason. Validate likely paths before reading. Remove stale pointers. Never copy facts into the set. Delete or ignore the local set when the task ends.
