# Template migrations

Each released migration will receive a versioned document describing prerequisites, changed contracts, file-level actions, verification, and rollback. This directory is machinery and release history, not a competing specification owner.
