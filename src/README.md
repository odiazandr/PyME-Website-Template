# Runtime source

The Astro runtime and validated business-data foundations are implemented. `pages/index.astro` is a minimal build proof that consumes canonical data, not the finished reference website. Later phases add layouts, components, styles, utilities, and production routes according to the canonical specifications.
