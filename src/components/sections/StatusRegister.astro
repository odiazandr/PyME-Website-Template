<aside class="status-register" aria-labelledby="status-register-title">
  <p id="status-register-title" class="status-register__title">
    Estado de la referencia
  </p>
  <dl>
    <div><dt>Arquitectura</dt><dd>Estática</dd></div>
    <div><dt>Datos</dt><dd>Esquema válido</dd></div>
    <div><dt>Publicación</dt><dd>No autorizada</dd></div>
  </dl>
</aside>

<style>
  .status-register {
    width: min(100%, 22rem);
    padding: var(--space-6);
    border: 1px solid var(--color-border-strong);
    background: var(--color-surface);
    box-shadow: var(--shadow-raised);
  }
  .status-register__title {
    color: var(--color-accent);
    font-size: var(--text-xs);
    font-weight: 750;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  dl {
    margin: var(--space-5) 0 0;
  }
  dl div {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-4);
    padding-block: var(--space-3);
    border-block-start: 1px solid var(--color-border);
  }
  dt {
    color: var(--color-text-muted);
  }
  dd {
    margin: 0;
    font-weight: 680;
    text-align: end;
  }
  @media (max-width: 47.99rem) {
    .status-register {
      width: 100%;
      max-width: none;
    }
  }
</style>
