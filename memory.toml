schema_version = 1
mode = "template"

[checks]
build = []
test = ["python", "-m", "unittest", "discover", "-s", "tests/memory"]
lint = []
secret_scan = []

[placeholders]
allow = []

[working_set]
enabled = true
directory = ".memory/working-sets"
maximum_pointers = 20
maximum_reason_length = 160
